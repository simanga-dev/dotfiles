
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################


# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/


# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto

monitor=,preferred,auto,1

monitor=HDMI-A-1,2560x1440@120.00Hz,auto,1

#
#

bindl=,switch:on:Lid Switch,exec,hyprctl keyword monitor "eDP-1, disable"

bindl=,switch:on:Lid Switch,exec,hyprctl keyword monitor "eDP-1, disable"


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nautilus
$menu = fuzzel

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = waybar
exec-once = wl-paste --watch cliphist store
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"   # for GTK4 apps



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 6
    gaps_out = 8

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = master
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 6

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    # drop_shadow = true
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
# master {
#     pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
#     preserve_split = true # You probably want this
# }

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
    new_on_top = true
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    new_window_takes_over_fullscreen = 1
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = meta # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

bind = $mainMod, return, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, return, exec, $terminal

bind = $mainMod, Q, killactive,
bind = $mainMod, o, exec, /home/simanga/.local/bin/quickmove.sh
bind = $mainMod control, Q, exit,
bind = $mainMod, N, exec, /home/simanga/.local/bin/notestaker.sh

# use to work for some reason it decides not to work anymore
bind = $mainMod, i, exec, /home/simanga/.local/bin/snippy.sh
bind = $mainMod control, i, exec, wl-copy -p wtype $(cliphist list | fuzzel -d | cliphist decode)


# bind = $mainMod, V, togglefloating,
bind = $mainMod, p, exec, $menu
bind = $mainMod shift, p, exec, fuzzel-pass.sh -ft
# bind = $mainMod, P, pseudo, # dwindle
# bind = $mainMod, J, togglesplit, # dwindle

bind = $mainMod, f, fullscreen, 1
bind = $mainMod shift, F, togglefloating,

bind=,F6,exec,hyprctl keyword monitor "eDP-1, disable"


bind = $mainMod, s, togglefloating,
bind = $mainMod, s, togglefloating,


# bind = $mainMod, m, togglegroup



bind = $mainMod, R, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, R, submap, exec

submap = exec

binde = , f, exec, zen-browser
binde = , f, submap, reset

binde = , t, exec, thunderbird
binde = , t, submap, reset

binde = , n, exec, alacritty -e nnnas
binde = , n, submap, reset


binde = , w, exec, whatsdesk
binde = , w, submap, reset

bind = , escape, submap, reset
submap = reset




# Window Management
# Cycle focus to next window in stack

# Window Management
# Cycle focus to next window in stack
bind = $mainMod, j, layoutmsg, cyclenext  # works only in master
bind = $mainMod SHIFT, j, layoutmsg, swapnext # works only in master
bind = $mainMod, k, layoutmsg, cycleprev   # works only in master
bind = $mainMod SHIFT, k, layoutmsg, swapprev # works only in master
bind = $mainMod control, j, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod control, j , workspace, e-1

bind = $mainMod control, k, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod control, k , workspace, e+1


bind = $mainMod, SPACE, layoutmsg, swapwithmaster master


# focus master window
bind = $mainMod, m, layoutmsg, focusmaster
# remove focused window from the master stack
bind = $mainMod, d, layoutmsg, removemaster

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l

bind = $mainMod, l, workspace, next
# bind = $mainMod, l, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 1, workspace, 1

bind = $mainMod, 2, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 2, workspace, 2

bind = $mainMod, 3, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 3, workspace, 3

bind = $mainMod, 4, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 4, workspace, 4

bind = $mainMod, 5, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 5, workspace, 5

bind = $mainMod, 6, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 6, workspace, 6

bind = $mainMod, 7, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 7, workspace, 7


bind = $mainMod, 8, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 8, workspace, 8

# bind = $mainMod, 9, togglespecialworkspace, off -- failed idea, force special window to hype when swithch table
bind = $mainMod, 9, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, 9, workspace, 9

# bind = $mainMod, 0, workspace, 10

bind = $mainMod, tab, exec, /home/simanga/.local/bin/close-special-window.sh
bind = $mainMod, tab, workspace, previous
bind = $mainMod, Backslash, togglespecialworkspace
bind = $mainMod control, Backslash, movetoworkspace, special

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$


# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*
windowrulev2 = workspace 9, class:(zen-alpha)
windowrulev2 = workspace 9, class:(firefox)
windowrulev2 = workspace 9, class:(Notes)
windowrulev2 = workspace 1, class:(Alacritty)
windowrulev2 = workspace 2, class:(Nemo)
windowrulev2 = workspace 4, class:(Gimp)
windowrulev2 = workspace 3, class:(org.gnome.Nautilus)
windowrulev2 = workspace 4, class:(Code)
windowrulev2 = workspace 4, class:(Inkscape)
windowrulev2 = workspace 7, class:(thunderbird)
windowrulev2 = workspace 4, class:(org.remmina.Remmina)
windowrulev2 = workspace 8, class:(whatsdesk)
windowrulev2 = workspace 8, class:(Slack)
windowrulev2 = workspace special, class:(NIDE)


# { "NIDE",                           NULL,       NULL,  SCRATCHPAD_MAS

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

workspace = 9, master:inherit_fullscreen, fullscreen:1
workspace = 8, fullscreen:1
